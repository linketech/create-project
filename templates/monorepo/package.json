{
  "name": "monorepo-project",
  "version": "1.0.0",
  "workspaces": [
    "brickyard_modules/*"
  ],
  "main": "index.js",
  "license": "MIT",
  "scripts": {
        "init": "yarn set version berry && yarn plugin import workspace-tools && yarn config set nodeLinker node-modules"
  }
}
